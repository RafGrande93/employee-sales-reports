# Employee Sales Reports

This project shows a simple modeling a company’s sales and running basic reports.

## Contents
- `company_schema_and_data.sql` – creates tables and inserts sample data  
- `company_with_total_sales_query.sql` – compute total sales per employee  
- `avg_sales_per_employee.sql` – compute average sale amount per employee  
